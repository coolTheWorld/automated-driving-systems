# =============================================================================
#  一帧的全部预测 —— ads_prediction 发布在 /prediction/trajectories
#
#  与 ObstacleArray 同一条外层 Header 的理由：下游需要知道"这一帧的时刻"
#  与"这一帧是不是完整的"。stamp = 触发本帧预测的感知帧时刻，
#  各条轨迹点的 t_s 都相对它。
#
#  数组为空是合法的：本帧没有任何目标（或全部目标无位移证据 ——
#  那时它们以 MODEL_STATIC 出现，所以"空"只在感知也为空时发生）。
# =============================================================================

std_msgs/Header header

PredictedTrajectory[] trajectories

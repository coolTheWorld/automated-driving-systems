# =============================================================================
#  轨迹上的一个点 —— 规划模块 → 控制模块
#
#  对应 SPEC §3.2 框图里 ads_planning → ads_control 的那条边。
#
#  为什么几何与速度打包在**一条**消息里，而不是「路径 + 另一个速度话题」：
#  拆成两个话题就有两个时间戳，控制侧要自己对齐。轨迹是一个整体 ——
#  「这条几何配这组速度」是规划器的一个决定，拆开就给了它们不一致的机会，
#  而不一致的症状是**车按新几何走、按旧速度开**，弯道上直接超横向加速度限值。
# =============================================================================

# 位置与朝向，map 系（ENU）。
#
# ⚠️ **这是后轴中心（base_link）的期望位姿，不是车体几何中心。**
#    与 ads_map 的路径、控制器的参考点保持一致（base_link 定在后轴中心是
#    Autoware 惯例，自行车模型/Stanley/纯追踪都以后轴为参考点推导）。
#    做碰撞检查时要自己沿车头方向前移 length/2 − rear_overhang（本项目 1.35 m）。
float64 x_m
float64 y_m
float64 heading_rad

# 曲率，1/m，**正 = 左转**（与 ads_map、ads_control 同一约定）。
#
# ⚠️ **由生产方给出，下游不要重算。** 控制侧有 ReferenceLine 会算一遍，
#    两边各算就有两个真值；绕障段（五次多项式解析曲率 vs 三点差分）
#    差得比中心线上大得多。
float64 curvature_1pm

# 沿**本条轨迹**的累计弧长，从 0 起，单位 m。**不是参考线的 s。**
# 按实际点距累加，不是「序号 × 采样步长」—— 后者在弯道上偏 14.58%（P2 实测）。
float64 s_m

# 该点的目标速度，m/s。已含曲率限速、终点/障碍物停车、前后向扫描。
float64 speed_mps

# 该点**起始**的目标加速度，a = ½·d(v²)/ds，单位 m/s²。给速度环做**前馈**。
#
# ⚠️ **语义是「本段内恒定」，不是该点的瞬时值。** 因此下游在段内插值时：
#      速度取 sqrt(v0² + ratio·(v1² − v0²))   ← v² 线性
#      加速度**直接取段起点的值**             ← 分段恒定
#    这是消息契约的一部分。写错不会报错，只会让前馈略偏。
#
# ⚠️ **不给它的后果是实测过的**：纯 P 速度环跟踪**斜坡**目标的稳态误差
#    = 斜率/K_p = 3.0/1.0 = 3.0 m/s，症状是「入弯偏快、终点冲过头」，
#    而稳态巡航时速度跟得很准（那时目标是常值）。见 docs/modules/control.md §4.4。
float64 accel_mps2

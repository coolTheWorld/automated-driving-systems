<?xml version="1.0"?>
<!-- Gazebo 模型元数据。model.sdf 是生成物，本文件不含任何几何，可以手写。 -->
<model>
  <name>campus_structures</name>
  <version>1.0</version>
  <sdf version="1.9">model.sdf</sdf>
  <description>
    园区路侧立体结构（路灯杆 + 建筑）。由 scripts/gen_map.py 从
    config/campus_map.yaml 生成 model.sdf —— 不要直接编辑 model.sdf。

    ⚠️ 它不是装饰，是 **P4 定位的前提条件**。

    点云对位姿的约束来自表面法向，一个平面只能约束沿它法向的那一个自由度。
    加这个模型之前，世界里只有 ground_plane 和 0.01 m 厚的路面板，
    所有点的法向都是 (0,0,1) —— NDT 只能约束 z / roll / pitch，
    而 x / y / yaw 完全不可观：点云在自己平面内滑动，配准残差一点不变。
    症状会是「NDT 怎么调都飘」，而错在世界文件里，不在配准代码里。

    它与 maps/campus_cloud.pcd 同源：NDT 用的先验点云就是从这些同样的
    几何解析采样出来的，所以「Gazebo 里的结构」和「NDT 以为的结构」
    不可能漂移。两者漂移的症状是 NDT **稳定地收敛到一个错误的位姿** ——
    残差很小、协方差很紧、看起来一切正常，车却偏着开。

    与 campus_road 不同，这里有 collision：可以开进去的路灯是个谎言，
    而且 P5 的感知要把它们当成真实障碍物。
  </description>
</model>

# =============================================================================
#  车辆控制指令 —— 控制模块 → 车辆（仿真或实车）
#
#  对应 SPEC §4.1 的规范话题 /vehicle_cmd。
#
#  为什么是「转角 + 加速度」而不是「转角 + 油门开度」：
#  油门开度是执行器层面的量，它和加速度的关系取决于当前车速、挡位、坡度、
#  载荷 —— 也就是说同一个油门开度在不同工况下产生不同的加速度。
#  控制器输出物理量（加速度），由更下层的执行器模型去解算需要多大油门，
#  这样纵向控制律就与具体车辆解耦了。CARLA 和 Gazebo 的执行器差异
#  也被挡在这一层之下，不会传到控制算法里（SPEC §4.1「行为漂移」）。
# =============================================================================

std_msgs/Header header

# 前轮转角，单位 rad。**不是方向盘转角**（两者差一个转向传动比）。
# 符号遵循 ROS 右手系绕 z 轴：**左转为正，右转为负**。
# 有效范围由 config/vehicle_params.yaml 的 limits.max_steer_angle_rad 决定
# （当前 ±0.600 rad ≈ ±34.4°）。超出范围时由 gazebo_bridge 截断并告警，
# 不是静默钳位 —— 静默钳位会让控制器的积分项一直往外飘而无人察觉。
float64 steer_angle_rad

# 期望纵向加速度，单位 m/s²。正 = 加速，负 = 减速。
# 有效范围同样来自 vehicle_params.yaml：
#   [-limits.max_decel_mps2, +limits.max_accel_mps2]，当前 [-3.0, +1.5]。
# 紧急制动（emergency_decel_mps2 = -5.0）只允许安全模块下发，
# 常规控制器不得越过 max_decel。
float64 accel_mps2

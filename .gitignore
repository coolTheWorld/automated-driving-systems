# colcon 构建产物
build/
install/
log/

# 由 scripts/setup_env.sh 生成，含机器相关的 UID/GID
.env

# 数据（rosbag 可能很大，不进 git）
logs/
*.mcap
*.db3

# core dump。仿真进程被 SIGKILL 时会在仓库根目录留下 core.<pid>，
# 一个就有几百 MB（Gazebo 驻留内存大），几次下来能到 GB 量级。
# 收尾请用 SIGINT + 进程组（见 CLAUDE.md 陷阱表），别直接 kill -9。
core.*

# 编辑器
.vscode/
.idea/
*.swp

# Python
__pycache__/
*.pyc

# =============================================================================
#  凭据 —— 绝不入库
#
#  ⚠️ 2026-08-03：仓库工作区里存在一个 `.ssh/`（含**私钥**，文件时间 07-28，
#     早于当次会话）。一次 `git add -A` 把它扫进了提交，**被 GitHub 的
#     push protection 拦下**才没推出去。
#
#  两条教训：
#  1. **`git add -A` 会把工作区里一切未忽略的东西都扫进来**，包括你不知道
#     什么时候放进来的凭据。它没有恶意，它只是听话。
#  2. 拦住它的不是我们自己的任何一道检查，是 GitHub 的。**本地没有防线** ——
#     所以补上这几行，让它连暂存都进不去。
#
#  私钥不该放在仓库目录里。这几行只是兜底，不是许可。
# =============================================================================
.ssh/
*.pem
*id_rsa*
*id_ed25519*

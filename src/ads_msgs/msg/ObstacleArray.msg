# =============================================================================
#  一帧内的全部障碍物
#
#  为什么要有外层 Header：数组里每个 Obstacle 自带 header，但下游需要知道
#  「这一帧的时刻」和「这一帧是不是完整的」。如果只发单个 Obstacle，
#  规划器无法判断某个目标是消失了还是这一帧还没发完 ——
#  而「目标消失」和「目标还没到」在避障里是两种完全不同的处理。
# =============================================================================

std_msgs/Header header

# 数组为空是合法的，表示「本帧确认没有障碍物」，不是「没检测」。
Obstacle[] obstacles
